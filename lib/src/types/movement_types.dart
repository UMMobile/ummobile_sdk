/// The possible movements type.
enum MovementTypes {
  Credit,
  Debit,
  Unknown,
}

/// The possible balance movements format type.
enum IncludeMovements {
  None,
  OnlyCurrent,
  CurrentAndLastYear,
}
